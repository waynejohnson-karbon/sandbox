name: Run every 10 minutes

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes

jobs:
  run_every_1_minutes:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Say hello
        run: echo "Hello! This workflow runs every 10 minutes."
